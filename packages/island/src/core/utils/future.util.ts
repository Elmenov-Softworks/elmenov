// type FutureTask<T> = (resolve: (value: T) => void, reject: (error: Error) => void) => void;

// export class Future<T> {
//   #task: FutureTask<T>;

//   constructor(task: FutureTask<T>) {
//     this.#task = task;
//   }
// }
